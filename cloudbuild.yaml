steps:
  # Step 1: Build Flutter APK using gke-deploy (Cloud Build native)
  - name: 'gcr.io/cloud-builders/gke-deploy'
    args:
      - 'run'
      - '--filename=.'
      - '--image=ubuntu:22.04'

timeout: '3600s'

options:
  logging: CLOUD_LOGGING_ONLY
